# Security and privacy note

- The project has no external runtime dependencies.
- It makes no requests to third-party services.
- It contains no authentication tokens, passwords or environment files.
- All business records are fictional and stored as static project data.
- The local server binds to `127.0.0.1`, not to the public network interface.
- The public demonstration performs calculations only in the visitor's browser.

